server {
    listen 80;
    server_name notes.battlefiesta.in www.notes.battlefiesta.in;

    root /var/www/studynotes;
    index index.html;

    access_log /var/log/nginx/studynotes_access.log;
    error_log /var/log/nginx/studynotes_error.log;

    # Static files
    location ~* \.(js|css|png|jpg|jpeg|svg|woff2?|ttf|ico)$ {
        try_files $uri =404;
    }

    # React / SPA routing
    location / {
        try_files $uri /index.html;
    }

    # API proxy (same backend)
    location /api {
        proxy_pass http://127.0.0.1:5000/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection upgrade;
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}