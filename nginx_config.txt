   server_name accusoft.battlefiesta.in www.accusoft.battlefiesta.in;

    root /var/www/accusoft;
    index index.html;

    access_log /var/log/nginx/accusoft_access.log;
    error_log /var/log/nginx/accusoft_error.log;

     location ~* \.(js|css|png|jpg|jpeg|svg|woff2?|ttf|ico)$ {
         limit_req zone=staticzone burst=8 nodelay;
         try_files $uri =404;
     }

    location / {
        limit_req zone=mylimit burst=3 nodelay;
        try_files $uri /index.html;
        # try_files $uri $uri/ =404;
    }

    location /api {
        proxy_pass http://127.0.0.1:5000/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection upgrade;
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }